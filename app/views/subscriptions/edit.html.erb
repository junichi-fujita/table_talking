<h1>編集する</h1>

<%= form_with(model: @subscription, url: recruitment_subscription_path(@recruitment, @subscription), local: true) do |form| %>
  <%= form.text_area :comment %>
  <%= form.submit "編集する" %>
<% end %>